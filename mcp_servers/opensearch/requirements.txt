# OpenSearch 巡检 MCP server 容器依赖。
# 迁移自 opensearch-project/opensearch-mcp-server-py(Apache-2.0),
# 依赖版本沿用 upstream pyproject.toml,补显式 ASGI runtime。

aiohttp>=3.13.4
boto3>=1.39.0
idna>=3.15
mcp[cli]>=1.9.4
opensearch-py==3.1.0
pydantic>=2.11.3
pyyaml>=6.0.2
requests>=2.33.0
requests-aws4auth>=1.3.1
semver>=3.0.4
pyjwt>=2.13.0
urllib3>=2.7.0

# 显式 ASGI runtime(streaming_server 用 uvicorn + starlette)
starlette>=0.40,<1.0
uvicorn>=0.30,<1.0
